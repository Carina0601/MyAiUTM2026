# File Structure
1. backend
- all the logic backend code

2. frontend 
a. index.html = render the JSX code into HTML so that the browser can show the frontend 
                normally it's just have a few lines of code
b. App.jsx = main file to write frontend react code
c. main.jsx = a connection between HTML and JSX 
d. package.json = like lego instruction book tell us what we tools we used
e. model = voice recorder thing

# terminal
- npm install = install all the tools needed in package.json
- so if the package.json is inside a folder we need to (cd folderName)
- npm run dev = run the code
- Python: py folder name = run the code
- React npm run dev = run the code